#include<stdio.h>
int main;
{
    int x, n, *p, sum;
    printf("enter no of elements in array : ");
    scanf("%d,n");

    for(i=1;i<=n,i++)
    {
        printf("Enter element %d",i);
        scanf("%d",x);
    }

}